# spec-sync 5.0 — generated by `specsync init`
# Committed: config.toml, sdd.json, version
# Ignored: backups, local config, hash cache (regenerated on each run)

backup-3x/
config.local.toml
hashes.json
change.lock
change-transaction.json
